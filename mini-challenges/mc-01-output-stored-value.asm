				LDA varStoredValue		; Loads the value of the variable varStoredValue contents into the accummalor
				OUT						; Output the conents of the accummalor
				HLT						; Output
varStoredValue	DAT 10					; value 10 stored in memory with variable name varStoredValue
