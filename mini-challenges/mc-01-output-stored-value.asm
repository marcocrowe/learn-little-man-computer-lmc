				LDA varStoredValue		; Loads the value of the variable varStoredValue contents into the accumulator
				OUT						; Output the contents of the accumulator
				HLT						; Output
varStoredValue	DAT 10					; value 10 stored in memory with variable name varStoredValue
